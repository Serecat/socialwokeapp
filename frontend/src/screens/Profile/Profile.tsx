import React from 'react';

const Profile: React.FC = () => {
  return (
    <div>
      <h2>Profile</h2>
      {/* TODO: Fetch and display user profile from /users API */}
    </div>
  );
};

export default Profile;
